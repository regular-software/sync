# @regular-software/sync-drizzle

Drizzle ORM adapter for the regular software sync engine.

This package uses the native `better-sqlite3` dependency. It is a 0.x package;
APIs may change between minor versions.
